/* ============================================================
   OSMO — initBasicFilterSetupMultiMatch
   Paste Osmo source here.
   Scoped to [data-filter-group] via DOM attributes — no
   parameter needed; the function reads the DOM directly.
   ============================================================ */



/* ============================================================
   INIT
   ============================================================ */
document.addEventListener('DOMContentLoaded', function () {
  initBasicFilterSetupMultiMatch();
});
